// Create a monitorsListArray array here

var monitorsListArray = ["Apple", "Peach", "Berry"];

// End of monitorsListArray array

function myMonitorsFunction(arr) {
    var newMonitorsList = [...arr];

    // Only change code below this line
    for(var i = 0; i < monitorsListArray.length; i++) {
        var sum = 0;
    for(var j = 0; j< monitorsListArray[i].length; j++);
        var newMonitorsList = monitorsListArray[j] + monitorsListArray;
    }
    return newMonitorsList;
}
    // Only change code above this line


console.log(myMonitorsFunction("newMonitorList[i][j]")); // Change this line
module.exports = myMonitorsFunction;